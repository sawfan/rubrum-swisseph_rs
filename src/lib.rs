//! Adapter layer between the `swisseph` crate and `rubrum` domain types.
//!
//! The goal of this crate is to provide small, explicit conversion helpers so
//! callers can keep Swiss Ephemeris-specific types at the boundary.

pub mod adapter;
