//! Temporary binary entrypoint.
//!
//! This repository is primarily meant to expose an adapter library. Keeping a
//! minimal `main` avoids breaking consumers that expect a binary crate.

fn main() {
    println!("rubrum_swisseph adapter crate");
}
